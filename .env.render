# Render.com Environment Configuration for CredNXT
# These variables are automatically set by Render or need to be configured in Render dashboard

# === REQUIRED VARIABLES ===
NODE_ENV=production
PORT=10000

# Database connection (automatically provided by Render PostgreSQL service)
# DATABASE_URL will be automatically set by Render PostgreSQL service

# Security secrets (MUST be configured in Render dashboard - DO NOT hardcode)
# JWT_SECRET=<set-in-render-dashboard>
# SESSION_SECRET=<set-in-render-dashboard>

# === OPTIONAL VARIABLES ===
# Uncomment and configure as needed for your deployment

# Email configuration (if implementing email notifications)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password

# External API keys (configure in Render dashboard as needed)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PUBLISHABLE_KEY=pk_live_...
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=+1234567890

# Monitoring and analytics
# SENTRY_DSN=https://...
# GOOGLE_ANALYTICS_ID=GA-...

# Feature flags
# ENABLE_NOTIFICATIONS=true
# ENABLE_ANALYTICS=true
# ENABLE_PAYMENTS=true

# Rate limiting and security
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# CORS_ORIGIN=https://your-frontend-domain.com

# Logging
# LOG_LEVEL=info
# LOG_FORMAT=json