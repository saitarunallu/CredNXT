# CredNXT P2P Lending Platform - Environment Configuration
# 
# SECURITY NOTE: Never commit real values to version control
# Use Replit Secrets for all sensitive configuration
#

# Firebase Client Configuration (Public - Safe to expose)
# Configure these in Replit Secrets for automatic loading
VITE_FIREBASE_API_KEY=your_firebase_web_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# Development Configuration
NODE_ENV=development
VITE_API_BASE_URL=http://localhost:5000/api

# REQUIRED SECRETS (Configure in Replit Secrets):
# 
# FIREBASE_WEB_API_KEY - Firebase Web API key for client authentication
# FIREBASE_CONFIG_JSON - Complete Firebase service account JSON for server operations
# 
# These secrets are automatically loaded and should NEVER be hardcoded in files.
# When remixing this project, users must configure their own Firebase project
# and add these secrets to Replit Secrets for the application to function.